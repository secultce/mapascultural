tags:
  - Eventos
summary: Atualiza de um evento.
description: | 
  Atualiza um evento existente com os dados fornecidos.
  
  **Importante:** Para atualizar um evento, é necessário estar autenticado com o usuário responsável pelo evento.
requestBody:
  content:
    application/json:
      schema:
        $ref: '../../requestBodies/event/post-complete.yaml'
responses:
  '201':
    description: Evento atualizado com sucesso.
    content:
      application/json:
        schema:
          $ref: '../../schemas/event.yaml'
